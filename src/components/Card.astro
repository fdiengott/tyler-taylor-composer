<div class="card">
    <div class="card-body">
        <slot />
    </div>
</div>

<style>
    .card {
        border-radius: 0.5rem;
        border: 1px solid var(--color-border-primary);
    }

    .card-body {
        padding: var(--spacing-250);
    }
</style>
