---
import { getEntry, render } from 'astro:content';
import About from './About.astro';

const bio = await getEntry('about', 'long-bio');
const { Content } = await render(bio as any);
---

<About bio={bio}>
    <Content />
</About>
---

